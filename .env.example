# Environment variables template
# Copy this file to .env and replace with actual values

# JWT Secret for authentication across microservices (use a random 64+ character string)
JWT_SECRET=

# Internal service authentication token (use a random string)
INTERNAL_TOKEN=

# MongoDB Configuration
MONGO_INITDB_ROOT_USERNAME=
MONGO_INITDB_ROOT_PASSWORD=
